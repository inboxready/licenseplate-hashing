package hxd.impl;

/**
	The mouse movement input handling mode.

	**JS/HTML5 note**:
	> Due to browser limitations, `restorePos` is ignored and always treated as `true`.
	>
	> Additionally, mouse mode will be forcefully ch